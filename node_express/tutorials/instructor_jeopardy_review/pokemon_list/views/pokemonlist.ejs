<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    This is the pokemon list:
    <ol>
        <% for (let i of pokemon) { %>
            <li>
                <%= i.name %> - <%= i.id %>
        <a href="http://localhost:3000/pokemon/<%= i.id %>">Click <%= i.name %></a>
            </li>
        <% } %>
    </ol>
</body>
</html>